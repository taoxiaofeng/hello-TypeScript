{
  /*
    tsconfig.json 是ts编译器的配置文件，ts编译器可以根据它的信息来对代码进行编译。
    “include” 用来指定哪些ts文件需要被编译
    “exclude” 定义需要排除在外的目录
    “extends” 定义被继承的配置文件
    “files”  指定被编译的文件列表，只有编译的文件少时用到
    “compilerOptions” 编译器选项，用来完成对编译的配置
    “target”  用来指定ts被编译的es版本
  */
  "compilerOptions": {
    "module": "CommonJS",
    "target": "ES2016",
    "sourceMap": false
  },
  "exclude": [
    "node_modules"
  ],
  "include": [
    "./src/**/*", /* src下面的任意目录下面的任意文件 */
  ]
}